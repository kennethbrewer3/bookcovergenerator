name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.2'
      SERVERPOD_PASSWORD_database: 'QG0r6sr5d3gyu550kYc7EXS6bT7c7sPf'
      SERVERPOD_PASSWORD_redis: 'EwHSdLrtZCnH9eI-K8gHeHhCINQPXcvJ'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: book_cover_designer_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: book_cover_designer_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: book_cover_designer_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: book_cover_designer_server
        run: serverpod generate

      - name: Run tests
        working-directory: book_cover_designer_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: book_cover_designer_server
        run: docker compose down -v
